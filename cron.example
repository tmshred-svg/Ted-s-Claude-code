# Run weekdays at 17:30 America/New_York after US close.
# Adjust path and timezone for your machine.
30 17 * * 1-5 cd /home/user/Ted-s-Claude-code && /usr/bin/env -S bash -lc '. .venv/bin/activate && python -m dashboard.daily >> data/cron.log 2>&1'
